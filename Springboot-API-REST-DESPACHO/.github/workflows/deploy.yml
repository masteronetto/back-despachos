name: CI/CD back-despachos

on:
  push:
    branches:
      - deploy

jobs:
  build-push-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Login a Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_TOKEN }}

      - name: Build y Push imagen
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME }}/back-despachos:latest .
          docker push ${{ secrets.DOCKER_USERNAME }}/back-despachos:latest

      - name: Deploy en EC2 Backend
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ec2-user
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            docker pull daniel0netto/back-despachos:latest
            docker stop back-despachos || true
            docker rm back-despachos || true
            docker run -d --name back-despachos -p 8081:8081 \
              -e DB_ENDPOINT=mysql \
              -e DB_PORT=3306 \
              -e DB_NAME=despachos_db \
              -e DB_USERNAME=despachos_user \
              -e DB_PASSWORD=despachos_pass \
              daniel0netto/back-despachos:latest